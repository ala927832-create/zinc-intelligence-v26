# V2.6 部署說明（GitHub Actions + GitHub Pages）

## 建議部署方式

1. 建立 **Private GitHub repository**。
2. 把本資料夾全部上傳到 repository 根目錄。
3. Repository → **Settings → Pages → Source → GitHub Actions**。
4. Repository → **Settings → Secrets and variables → Actions**，視你的資料來源加入：
   - `DISCORD_WEBHOOK_URL`
   - `ZINC_MARKET_API_URL`
   - `ZINC_MARKET_API_KEY`
   - `ZINC_DAILY_CSV_URL`
   - `ZINC_1H_CSV_URL`
   - `ZINC_15M_CSV_URL`
5. 第一次執行：Actions → **Zinc Intelligence V2.6 → Run workflow**。
6. 輸入：
   - `current_inventory_t`：目前可用鋅庫存（噸）
   - `demand_60d_t`：未來 60 天需求（噸）
7. 之後若某天留白，系統沿用上一次有效值，並顯示資料年齡與狀態。

## 網站與企業資料安全

`data/` 會保留企業庫存、60 天需求、歷史採購狀態，因此 **repo 應使用 private**。

一般 GitHub Pages 網站即使來源 repo 為 private，也可能仍是公開網站。V2.6 因此預設在 Pages 上遮蔽：

- 現有庫存精確數量
- 60 天需求精確數量
- 未覆蓋需求噸數
- 建議覆蓋噸數
- 對應 LME lot 數

如果你使用具備私密 Pages 存取控制的內部環境，才考慮設定：

`PUBLIC_DASHBOARD_INCLUDE_PRIVATE=true`

## LME / Zinc 核心資料

正式版不會製造假 LME K 線。

你需要提供：

- 合法授權或企業既有的 LME / Zinc API，設定 `ZINC_MARKET_API_URL`；或
- 更新 `data/manual_market.json`；
- LME Zinc 3M OHLC 則可提供 CSV / CSV URL。

Daily CSV 格式：

```csv
timestamp,Open,High,Low,Close,Volume
2026-09-10T18:00:00Z,....
```

時間原則：

- 儲存：UTC
- 交易所時區概念：Europe/London
- 顯示：Asia/Taipei

## 每日流程

```text
Market / Macro / Event data
        ↓
Data Quality Gate
        ↓
Market Regime + 5D / 20D / 60D
        ↓
Procurement Book
        ↓
Conservative / Aggressive Paper Research
        ↓
Freeze Daily Signal
        ↓
Paper Trade Pending → Next Session Entry
        ↓
MAE / MFE / Stop / Target / Time Exit
        ↓
Strategy Performance + Research Lab
        ↓
Discord + GitHub Pages
```

## Paper Trading 原則

投資模組僅為研究與模擬：

- 不含 broker API
- 不含真實下單
- 不含槓桿帳戶登入
- 訊號在當日 Freeze 後，模擬於下一個可執行時段進場
- 同一根 Daily K 同時碰 Stop 與 Target 時，採較保守的 Stop-first 假設
- 保存 MAE / MFE / P&L / R / Model Version

## 網站頁面

- `index.html`：每日 Dashboard
- `research.html`：Paper Trade / Strategy Research Lab

網站由工作流程自動生成於 `public/`，再部署至 GitHub Pages。
